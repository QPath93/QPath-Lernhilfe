<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>Meine Lernhilfe erstellen</title>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap');

    /* Grundstyles + Darkmode */
    body {
      font-family: 'Inter', sans-serif;
      margin: 0;
      padding: 0;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      min-height: 100vh;
      transition: background 0.3s, color 0.3s;
    }
    body.light {
      background: linear-gradient(135deg, #e0e7ff, #f0f4ff);
      color: #333;
    }
    body.light .container {
      background: #fff;
      color: #333;
      box-shadow: 0 8px 20px rgba(0,0,0,0.1);
    }
    body.dark {
      background: linear-gradient(135deg, #111827, #1f2937);
      color: #e5e7eb;
    }
    body.dark .container {
      background: #1f2937;
      color: #f3f4f6;
      box-shadow: 0 8px 20px rgba(0,0,0,0.4);
    }

    .container {
      max-width: 800px;
      width: 90%;
      margin: 40px auto;
      padding: 30px;
      border-radius: 12px;
      transition: background 0.3s, color 0.3s;
    }

    .logo {
      display: block;
      margin: 0 auto 15px;
      max-width: 180px;
    }

    /* Zurück-Button */
    .back-btn {
      display: inline-block;
      margin-bottom: 20px;
      padding: 8px 16px;
      background: transparent;
      color: #3b82f6;
      text-decoration: none;
      border: 2px solid #3b82f6;
      border-radius: 8px;
      font-weight: 600;
      transition: background 0.3s, color 0.3s;
    }
    .back-btn:hover {
      background: #3b82f6;
      color: #fff;
    }

    h1 {
      text-align: center;
      margin-bottom: 20px;
      font-weight: 600;
    }

    label {
      font-weight: 600;
      display: block;
      margin-top: 15px;
      margin-bottom: 5px;
    }

    input[type="text"],
    textarea,
    input[type="password"],
    select {
      width: 100%;
      padding: 10px;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 14px;
      margin-bottom: 10px;
      box-sizing: border-box;
    }

    .hilfe-box {
      padding: 15px;
      border: 1px solid #4b5563;
      border-radius: 8px;
      margin-bottom: 20px;
      position: relative;
    }

    .remove-btn {
      position: absolute;
      top: 12px;
      right: 12px;
      background: #ef4444;
      color: #fff;
      border: none;
      border-radius: 6px;
      padding: 4px 8px;
      cursor: pointer;
    }

    .add-btn,
    .submit-btn {
      display: inline-block;
      margin: 10px 5px 0 0;
      padding: 12px 20px;
      font-size: 16px;
      font-weight: 600;
      border-radius: 8px;
      cursor: pointer;
      transition: background 0.3s;
      border: none;
    }
    .add-btn {
      background: #3b82f6;
      color: #fff;
    }
    .add-btn:hover {
      background: #2563eb;
    }
    .submit-btn {
      background: #10b981;
      color: #fff;
    }
    .submit-btn:hover {
      background: #059669;
    }

    .dropzone {
      border: 2px dashed #3b82f6;
      padding: 10px;
      text-align: center;
      border-radius: 8px;
      cursor: pointer;
      margin-bottom: 10px;
      transition: background 0.2s;
    }
    .dropzone.dragover {
      background: rgba(59,130,246,0.1);
    }

    .file-list {
      list-style: none;
      padding: 0;
      margin: 0;
    }
    .file-list li {
      margin: 5px 0;
      font-size: 14px;
    }
    .file-list button {
      background: none;
      border: none;
      color: red;
      cursor: pointer;
    }

    .hint-icon, .hint-icon2 {
      display: inline-block;
      width: 18px;
      height: 18px;
      background: #3b82f6;
      color: #fff;
      border-radius: 50%;
      text-align: center;
      font-size: 12px;
      line-height: 18px;
      margin-left: 5px;
      cursor: pointer;
      position: relative;
    }
    .hint-icon:hover::after {
      content: "Mit der Codesperre können Hilfen gesperrt werden…";
      position: absolute; top: -5px; left: 30px;
      width: 250px; background: rgba(0,0,0,0.8);
      color: #fff; padding: 8px; border-radius: 8px;
      font-size: 12px; line-height: 1.4; z-index: 100;
    }
    .hint-icon2:hover::after {
      content: "Dateien können per Drag & Drop hinzugefügt werden.";
      position: absolute; top: -5px; left: 30px;
      width: 250px; background: rgba(0,0,0,0.8);
      color: #fff; padding: 8px; border-radius: 8px;
      font-size: 12px; line-height: 1.4; z-index: 100;
    }

    .theme-toggle {
      position: fixed;
      top: 15px;
      right: 15px;
      width: 36px; height: 36px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(0,0,0,0.1);
      border-radius: 50%;
      font-size: 18px;
      cursor: pointer;
      transition: background 0.3s;
      backdrop-filter: blur(4px);
      z-index: 10;
    }
    body.light .theme-toggle { color: #111; }
    body.dark  .theme-toggle { color: #fff; background: rgba(255,255,255,0.1); }
    .theme-toggle:hover { background: rgba(0,0,0,0.2); }
  </style>
</head>
<body class="light">
  <div class="theme-toggle" id="themeToggle">🌙</div>

  <div class="container">
    <img src="/logo.png" alt="Logo" class="logo">
    <a href="/dashboard" class="back-btn">Zurück zu meinen Lernhilfen</a>
    <h1>Meine Lernhilfe erstellen</h1>

    <form id="lernForm" action="/create" method="POST" enctype="multipart/form-data">
      <label for="title">Titel der Lernhilfe</label>
      <input type="text" name="title" id="title" required>

      <label for="subject">Fach auswählen</label>
      <select name="subject" id="subject" onchange="toggleCustomSubject()" required>
        <option value="">– Bitte wählen –</option>
        <option>Biologie</option>
        <option>Chemie</option>
        <option>Deutsch</option>
        <option>Englisch</option>
        <option>Französisch</option>
        <option>Geschichte</option>
        <option>Geographie</option>
        <option>Gesellschaft</option>
        <option>Physik</option>
        <option>Informatik</option>
        <option>Kunst</option>
        <option>Latein</option>
        <option>Mathematik</option>
        <option>Musik</option>
        <option>Naturwissenschaften</option>
        <option>Naturwissenschaft und Technik</option>
        <option>PGW</option>
        <option>Philosophie</option>
        <option>Politik</option>
        <option>Psychologie</option>
        <option>Spanisch</option>
        <option>Sport</option>
        <option>Religion</option>
        <option value="Sonstiges">Sonstiges</option>
      </select>

      <div id="customSubjectWrapper" style="display:none; margin-top:10px;">
        <label for="customSubject">Fach (frei eingeben)</label>
        <input type="text" name="customSubject" id="customSubject"
               placeholder="z. B. Technik, Philosophie...">
      </div>

      <div id="hilfen-container"></div>

      <button type="button" class="add-btn" id="addHilfeBtn">+ Weitere Hilfe hinzufügen</button>
      <button type="submit" class="submit-btn">Lernhilfe erstellen</button>
    </form>
  </div>

  <script>
    // Zusätzliche Hilfen
    const hilfenContainer = document.getElementById('hilfen-container');
    const addHilfeBtn    = document.getElementById('addHilfeBtn');
    let hilfeCount = 0;
    function createHilfeBox(i) {
      const box = document.createElement('div');
      box.className = 'hilfe-box';
      box.innerHTML = `
        <button type="button" class="remove-btn"
          onclick="this.parentElement.remove()">❌ Entfernen</button>
        <label>Name der Hilfe</label>
        <input type="text" name="hilfen[${i}][name]" placeholder="Name der Hilfe">
        <label>Inhalt</label>
        <textarea name="hilfen[${i}][content]" rows="3"
                  placeholder="Beschreibung oder Erklärung"></textarea>
        <label>
          <input type="checkbox" name="hilfen[${i}][locked]">
          Mit Code sperren?<span class="hint-icon">?</span>
        </label>
        <input type="password" name="hilfen[${i}][lockCode]"
               placeholder="Entsperrcode (optional)">
        <label>Dateien<span class="hint-icon2">?</span></label>
        <div class="dropzone" data-index="${i}">
          <p>Dateien hier ablegen oder klicken</p>
          <input type="file" name="hilfen[${i}][file]" multiple hidden>
        </div>
        <ul class="file-list" data-index="${i}"></ul>
      `;
      return box;
    }
    addHilfeBtn.addEventListener('click', () => {
      const box = createHilfeBox(hilfeCount++);
      hilfenContainer.appendChild(box);
      initDropzone(box.querySelector('.dropzone'));
    });
    addHilfeBtn.click();

    function initDropzone(dz) {
      const idx = dz.dataset.index;
      const inp = dz.querySelector('input[type=file]');
      const list= document.querySelector(`.file-list[data-index="${idx}"]`);
      let files = [];
      dz.addEventListener('click', ()=> inp.click());
      inp.addEventListener('change', e=> addFiles([...e.target.files]));
      dz.addEventListener('dragover', e=>{ e.preventDefault(); dz.classList.add('dragover'); });
      dz.addEventListener('dragleave', ()=> dz.classList.remove('dragover'));
      dz.addEventListener('drop', e=> {
        e.preventDefault();
        dz.classList.remove('dragover');
        addFiles([...e.dataTransfer.files]);
      });
      function addFiles(newF) {
        newF.forEach(f=>{
          if (!files.find(x=> x.name===f.name && x.size===f.size)) files.push(f);
        });
        updateList();
      }
      function updateList() {
        list.innerHTML='';
        const dt=new DataTransfer();
        files.forEach((f,i)=>{
          const li=document.createElement('li');
          li.textContent=f.name+' ';
          const b=document.createElement('button');
          b.textContent='❌';
          b.onclick=()=>{ files.splice(i,1); updateList(); };
          li.appendChild(b);
          list.appendChild(li);
          dt.items.add(f);
        });
        inp.files=dt.files;
      }
    }

    function toggleCustomSubject() {
      const sel = document.getElementById('subject').value;
      const w   = document.getElementById('customSubjectWrapper');
      const i   = document.getElementById('customSubject');
      if (sel==='Sonstiges') {
        w.style.display='block'; i.required=true;
      } else {
        w.style.display='none'; i.required=false; i.value='';
      }
    }

    // Dark/Light Switch
    const themeToggle = document.getElementById('themeToggle');
    const bodyElem    = document.body;
    function applyTheme(t) {
      bodyElem.classList.replace(t==='dark'?'light':'dark', t);
      themeToggle.textContent = t==='dark' ? '☀️' : '🌙';
      localStorage.setItem('theme', t);
    }
    themeToggle.addEventListener('click', ()=>
      applyTheme(bodyElem.classList.contains('light')?'dark':'light')
    );
    applyTheme(localStorage.getItem('theme')||'light');
  </script>
</body>
</html>